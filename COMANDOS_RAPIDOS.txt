# Levantar stack

docker compose -f docker-compose-ollama.yml up -d

# Health
curl http://localhost:11435/health

# Modelos
curl http://localhost:11435/v1/models

# Probar inferencia real
curl -X POST http://localhost:11435/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\":\"gemma3\",\"messages\":[{\"role\":\"user\",\"content\":\"Que es Docker?\"}]}"

# Descargar modelo en Ollama (si no existe)
docker exec -it kernelia-ollama ollama pull gemma3
